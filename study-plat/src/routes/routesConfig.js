import Onboard from "../containers/Onboard/Onboard"

const routesConfig = [
    // Onboard Page
    {
        path: '/onboard',
        exact: true,
        element: <Onboard />
    }
]

export default routesConfig